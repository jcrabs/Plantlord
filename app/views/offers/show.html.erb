<div class="container">
  <h1><%= @offer.plant_name %><h1>
  <p><%= @offer.price %> €</p>
  <p>this is the price breakdown</p>
  <p><%= @offer.plant_description %></p>
  <%= @offer.user_id %>
  <%= cl_image_tag @offer.photo.key, height: 300, width: 400, crop: :fill %>
</div>
